for (let i = 1; i <= 5; i++) {
    let row = "      ";            // start with empty line

    for (let j = 1; j <= i; j++) {
        row += "*";          // add stars
    }

    console.log(row);        // print the row
}
